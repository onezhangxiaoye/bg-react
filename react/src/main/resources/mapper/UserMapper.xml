<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.react.pojo.UserMapper" >

    <!-- 根据 ID 查询用户数据 -->
  <select id="selectByPrimaryKey" resultType="com.react.entity.User" parameterType="Integer" >
    select 
      *
    from user
    where id = #{id}
  </select>
    <!-- 查询所有用户数据 -->
  <select id="selectAllData" resultType="com.react.entity.User" >
    select
      *
    from user
  </select>
  <!-- 根据 用户名 查询用户数据 -->
  <select id="selectByUserName" resultType="com.react.entity.User" parameterType="String" >
    select
      *
    from user
    where name = #{name}
  </select>
  <!--注册用户信息-->
  <insert id="register" parameterType="com.react.entity.User" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
    insert into
      user
     (name ,password,email)
    values
     (#{name},#{password}, #{email});
  </insert>

</mapper>